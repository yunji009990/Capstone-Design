# 다시, 봄 — 공통 AI 작업 지침

기준일: 2026-09-15. 이 파일이 도구에 공통으로 적용할 프로젝트 지침이다.
사용자의 현재 지시와 실제 세션의 도구·권한을 우선한다. 다른 PC의 권한 제한을 현재 환경에 일반화하지 않는다.

## 현재 담당과 목표

**이 작업선의 기본 개발 범위는 대화 AI다. T포즈·3D 모델 제작은 팀원이 담당한다.**
대화 AI는 `AI_Response_Test`에서 개발하고, 팀원은 별도 clone의 `Tripo_Model_Test`에서 모델을 개발한다.
`Scene_1`, `Scene_2`는 당분간 기능 개발 대상에서 제외한다.
팀원이 Tripo 작업 지시서를 명시적으로 전달한 세션에서는 그 담당 범위를 따른다.

- 현재 흐름: Unity 입력 → SenseVoiceSmall/STT·VAD → 의도 판정 → Gemma → Qwen3-TTS → Unity 음성·자막.
- 의미 기반 끼어들기 정책은 `semantic_v1`이다. 일반/추론은 현재 같은 Gemma의 thinking 설정 차이다.
- 2026-09-14 사용자 요청으로 TTS를 재연결했다. `DIALOGUE_TTS_URL=http://127.0.0.1:8003`이며 Qwen3-TTS Base를 사용한다.
- 같은 날 생성 중 PCM 전송으로 전환했다. TTS API 8003이 별도 `qwentts-stream` venv의 vLLM-Omni 엔진 8004를 관리한다.
  현재 설치·예열·복구·검증은 [TTS 실시간 스트리밍](docs/TTS_실시간_스트리밍.md)을 따른다.
- 2026-09-15 페르소나별 대기 대사·음성을 사전 준비해 추론 중 재생한다.
  [캐릭터 대기 리액션](docs/캐릭터_대기_리액션.md)을 따른다. 첫 리액션 시간과 첫 본답변 시간을 구별한다.
- 같은 날 사용자 요청으로 웹 설문 변경 시 설정 재작성, 기본 예시 제거, 서버 UUID4 세션 발급을 적용했다.
  [웹 등록 흐름 개선](docs/웹_등록_흐름_개선.md)을 따른다. 관계별 말투·이름 예문 문제는 후속 논의로 보류했다.
- 눈맞춤·몸짓 반응·동작 따라 하기 등 VR 추가 기능은 현재 음성 대화 안정화 이후로 보류한다.
- Raon 모델·전용 환경·구형 실행기는 폐기했다. 과거 문서의 실행 명령으로 복원하지 않는다.
- Unity는 2022.3.62f2다. 기존 개발선은 `jw`이며 실제 브랜치는 작업 시작 때 확인한다.

## 먼저 읽을 문서

| 목적 | 문서 |
|---|---|
| 하네스 사용·검사·완료 기준 | [AI 하네스](docs/AI_하네스.md) |
| 대화 AI 담당의 작업 순서 | [대화 AI 개발 가이드](docs/대화_AI_개발가이드.md) |
| 대화 구조·모델·취소 계약 | [서버 음성 대화 구조](docs/서버_음성대화_구조.md) |
| 핵심 기억·정정·삭제 | [대화 메모리](docs/대화_메모리.md) |
| TTS 설치·예열·복구 | [TTS 실시간 스트리밍](docs/TTS_실시간_스트리밍.md) |
| 캐릭터별 대기 음성·최신 검증 | [캐릭터 대기 리액션](docs/캐릭터_대기_리액션.md) |
| 실제 Unity 검사 | [AI 응답 테스트 씬](docs/AI_응답_테스트_씬.md) |
| 판정기 실제 사례 | [판정기 텍스트 검사](docs/판정기_텍스트_검사.md) |
| 서비스 경계·배포 | [서비스 분리](docs/웹_Tripo_대화AI_서비스_분리.md), [서버 운영](Server/README.md) |
| 웹 설문·등록 계약 | [등록 흐름 개선](docs/웹_등록_흐름_개선.md), [설문 항목 조사](docs/웹_설문_항목_사용현황.md) |
| 팀원 Tripo 범위 | [팀원 작업 지시서](docs/Tripo_팀원_AI_작업지시서.md), [실행 가이드](docs/Tripo_개발환경_실행가이드.md) |

`docs/Raon_작업이력_보관.md`와 그 문서가 연결하는 과거 실험은 가설·실측 근거를 찾을 때 읽는다.
과거 실험과 현재 구현을 구분한다. 통합 설계안의 제안 API를 이미 구현된 API로 설명하지 않는다.

## 파일 소유와 연결 계약

| 담당 | 주된 수정 범위 |
|---|---|
| 대화 AI | `Server/dialogue_*.py`, `persona_client.py`, `realtime_*.py`, `interruption_policy.py`, `persona_context.py`, `voice_reference.py`, `tts_*.py`, TTS 엔진 설정·설치, 대화 검사·설정 예시 |
| 대화 AI Unity | `Assets/Scripts/Dialogue/`, `Assets/Editor/DialogueTestScene*`, `Assets/Scenes/AI_Response_Test.unity` |
| T포즈·3D 팀원 | 제작용 `Web/`, `Survey/`, `tools/tripo_trial.py`, `tools/tripo_motion_pack.py`, Tripo Editor 도구·`Tripo_Model_Test.unity` |
| 공통 | 하네스·문서·검사 실행기·인계 도구, `Server/registration/`, `service.sh`, `Assets/Scripts/Persona/PersonaSpawner.cs`, 패키지·공통 설정 |

대화 개발 중 팀원의 제작 기능으로 범위를 넓히지 않는다. 공통 코드는 현재 요청에 필요한 최소 범위만 고치고
제공자·소비자 계약 검사를 함께 실행한다. 기존 사용자 지시로 필요한 변경이 승인된 경우 같은 확인을 반복하지 않는다.

- 대화 AI는 등록 API에서 인물 정보를 읽는다. Web/Survey를 import하거나 운영 인물 폴더를 직접 수정하지 않는다.
- 인물 조회 계약: `schema_version`, `session`, `revision`, `persona`, `knowledge`, `rules`, `voice_sha256`.
- 신규 `/session/start`는 서버 발급 ID만 사용한다. 반환된 `session`을 저장하며 기존 ID 지정·덮어쓰기는 거부한다.
  웹의 `survey_revision`은 설문 일치 검사이고, 인물 조회의 `revision`과는 별개다.
- 응답의 `response_id`, 입력/재생 턴, 전달 확인된 답변, 보류·재개·취소 의미를 보존한다.
- Unity 테스트 씬은 각자 접속 설정을 쓴다. Scene_2나 Tripo 설정을 AI 테스트의 초기값으로 가져오지 않는다.
- Unity는 같은 기본 어셈블리를 사용한다. 씬 분리가 컴파일 격리를 뜻하지 않는다. 팀원별 clone과 `.meta` GUID를 보존한다.

## 작업 절차

1. `git status`, 현재 코드, 해당 기준 문서를 확인한다. 작업 전부터 있던 변경과 이번 변경을 구분한다.
2. 재현 조건과 기대 동작을 정하고 담당 영역에서 수정한다. 기능 검사 통과와 실제 답변 품질을 구별한다.
3. 기본 검사: `python tools/check.py --area dialogue`. 하네스만 수정하면 `--area harness`, 공통 경계를 수정하면 `--area all`도 확인한다.
4. Unity 코드를 바꿨다면 정확한 프로젝트의 MCP 연결·컴파일·Console과 AI 테스트 메뉴를 확인한다.
5. 변경 내용, 검사 결과 파일, 실제 모델/Unity 검사 여부, 남은 제한을 기록한다. 통과한 검사는 새 변경이나 실패 근거 없이 반복하지 않는다.

검사 실행기는 저장소 위치를 스스로 찾는다. Python 환경 선택과 새 PC 설치는 하네스 문서를 따른다.
검사 실패·미실행을 통과로 바꾸거나 운영 검사를 모의 검사 결과로 대신하지 않는다.

## 서버와 자료

- 대화 코드: `~/capstone-server`, API 8002. LLM은 기존 8001, TTS는 8003 loopback의 별도 프로세스다.
- 웹·등록·Tripo: `~/webapp`, 웹 8500·등록 8000·별도 Tripo 작업자. 대화 개발을 위해 재시작하지 않는다.
- SSH 별칭 `raon`은 이 PC의 접속 설정이다. 팀원은 자기 접속 권한과 별칭을 준비한다.
- 대화 제어는 `~/capstone-server/dialogue.sh api {start|stop|status}`다. 기존 `start.sh`는 등록 서비스 호환 명령이다.
- 배포가 작업 범위에 포함될 때 `tools/service_bundle.py dialogue`로 대상 파일을 확인하고 백업·해시·health·로그를 대조한다.
- 실행 중인 LLM과 다른 사용자의 GPU 프로세스를 임의로 종료하지 않는다. 테스트용으로 현재 등록 인물을 바꾸지 않는다.
- 실제 `.env`, 키·토큰, 참여자 사진·음성·세션, 생성 GLB는 Git·프롬프트·결과 보고에 복사하지 않는다.
  설정 확인은 변수명·설정 여부·연결 결과로 보고한다. 참조 음성의 출처·길이·전사를 확인한다.

## 도구와 협업

- 기본은 메인 단독이다. 사용자가 보조 에이전트나 `/verify`, `/impl`을 요청한 경우에만 해당 독립 작업을 위임한다.
  보조 역할은 읽기·진단·편집안이며 적용은 메인이 담당한다. Unity 편집은 메인에서 진행한다.
- 에이전트 기능이 없는 환경에서는 메인이 진행하고, 수행하지 않은 독립 검증을 했다고 보고하지 않는다.
- 현재 세션의 실제 읽기·쓰기·셸 권한을 따른다. 특정 모델이나 샌드박스 설정으로 임의 변경하지 않는다.
- Unity 작업에 Git worktree를 만들지 않는다. 다른 작업자의 변경을 덮어쓰거나 자동으로 되돌리지 않는다.
- 검색은 `rg`, Git은 작업 디렉터리 옵션 또는 `git -C <저장소> ... -- <경로>`를 쓴다.
  경로와 인수를 안전하게 전달하며 Windows 재귀 삭제는 절대 경로와 대상 범위를 확인한다.
- 한국어 출력은 UTF-8을 사용한다. 소스·문서는 한국어 설명을 기본으로 한다.
- 비교 실험은 한 번에 한 변수를 바꾼다. 음성 품질 비교는 소스 3개·회차 3회 이상과 청취 판정을 구분해 기록한다.
  대화 품질은 여러 턴의 인물·상황·기록을 유지해 평가한다. 확실하지 않은 결론은 미확정으로 남긴다.

