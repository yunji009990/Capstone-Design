# 2026-09-15 전체 검증 결과

아래는 **웹 등록 흐름 수정 전** 커밋 `8fbd6ea`의 전체 검사 기록이다.
같은 날 이후 설문 재작성·자동 세션 ID를 적용한 최신 회귀 검사 135개와 브라우저·서버 확인은
[웹 등록 흐름 개선](웹_등록_흐름_개선.md)을 따른다. 아래 125개 결과와 당시 지연 수치는 그대로 보존한다.

해당 커밋의 자동 검사와 실제 서버·Unity 기능 검사는 모두 통과했다.
추론 본답변 지연에는 편차가 있으며, Quest Pro 실기·APK 빌드·청취 품질까지 검증한 결과는 아니다.

검사 기준은 `jw`의 `8fbd6ea5ec922b4f369f0e2a065857ed1aa5b46f`다. 시작할 때 작업 트리는 깨끗했다.
2026-09-15 13:39부터 14:01까지 KST 기준으로 검사했고, 아래 수치는 이번 실행에서 새로 얻은 결과다.
운영 코드·Unity 소스·씬·패키지를 수정하거나 서비스를 재시작하지 않았다.

| 검사 | 이번 결과 | 확인한 범위 |
|---|---|---|
| 전체 모의 검사 | **125/125**, 건너뜀 0 | 하네스 11, 서버 109, Tripo 작업자 5. 등록 API 계약·기억·스트리밍·취소·리액션 포함 |
| 실제 Gemma 판정기 | **68/68** | 일반/추론 분류 24, 끼어들기 행동·경로 44 |
| 연결 내 핵심 기억 | **60턴, 9개 조건 통과** | 최근 대화 범위를 벗어난 정보 회상, 정정, 선택 삭제, 다른 기억 유지, 초기화 |
| 되물음의 답변 저장 | **5개 시나리오·199턴 통과** | 짧은 답변과 질문의 연결, 저장 묶음 경계, 정정, 두 주제 구분, 재확인 없이 나중에 회상 |
| 실제 음성 왕복 | **9/9** | PCM 입력 → VAD·STT → 판정·Gemma → Qwen3-TTS PCM 수신 |
| 실제 TTS와 기억 연동 | **7턴, 6개 조건 통과** | 음성이 나오는 상태에서 기억 조회·정정·선택 삭제·초기화 |
| Unity 대화 기능 | **4개 검사 메뉴 통과** | 참조 음성 미리 듣기, 참조 업로드 후 음성 왕복, 텍스트·상황 전달, 끼어들기 4개 경로 |
| 기존 Tripo 모델 | **2개 모델 × 9개 동작 × 편집/Play 모드 통과** | 모델별 뼈 41개, 유효한 메시, 샘플링 및 시간 경과에 따른 뼈·정점 이동 |
| 서버 종료 상태 | **정상** | 대화·TTS·등록 ready, 웹 HTTP 200, 테스트 연결·TTS 참조 정리 |

Unity Test Runner의 EditMode·PlayMode 작업도 실행했으나 실제 테스트 사례는 각각 **0개**였다.
이 결과는 통과 개수에 넣지 않았다. Unity 기능 검증의 근거는 프로젝트의 기존 Editor 메뉴가 생성한 결과와 AudioSource 출력이다.

실제 음성 왕복은 공개·합성 입력 3종을 각각 3회 전송했다. 일반 응답 6회와 추론 응답 3회로 나뉘었다.
아래 시간은 **입력 WAV의 마지막 PCM 패킷 전송부터 Windows 클라이언트가 첫 PCM을 수신할 때까지**다.
종료 무음 처리·서버 처리·왕복 네트워크가 포함되고, 최초 연결 준비와 Unity/헤드셋의 실제 출력 지연은 제외된다.
WAV 안의 끝 무음 길이에 따라 사람의 실제 발화 종료 시점과 차이가 날 수 있다.

| 응답 종류 | 첫 리액션 음성 | 첫 본답변 음성 |
|---|---|---|
| 일반, 6회 | 생략 | 평균 **1.15초**, 범위 0.96–1.35초 |
| 추론, 3회 | 평균 **1.29초** | 평균 **6.81초**, 범위 **3.64–9.55초** |

추론 3회에서 리액션은 각각 한 번 나왔고 일반 응답에서는 나오지 않았다.
리액션 뒤에 본답변 PCM이 이어지는 순서와 누적 샘플 경계도 일치했다.
이번 음성 측정은 대사·음성 캐시가 준비된 상태였다. 캐시가 없는 첫 인물의 준비 시간은 이 수치로 판단하지 않는다.
리액션은 기다리는 동안의 첫 반응이며 본답변 생성 시간을 줄인 것으로 해석하지 않는다.
추론 3회의 답변 내용·길이가 달라, 이전 실행과의 평균 차이만으로 성능 회귀를 단정하지 않는다.

판정기 68개 호출의 지연 중앙값은 0.465초, p95는 0.609초였다. 이 수치는 텍스트 판정만의 시간이다.
Unity 참조 업로드 왕복에서는 `reference_icl`과 실제 출력 신호를 확인했다.
Unity 화면의 첫 본답변 음성 수치는 0.689초였으며, 서버의 발화 종료 판정 이후를 재므로 위 1.15초와 시작점이 다르다.

기억 검사의 실제 답변도 함께 검토했다. AI는 알려 주지 않은 여행지를 되물었고,
사용자가 짧게 답한 제주도를 최근 30턴 밖에서도 회상했다. 발표 요일은 정정한 금요일로 답했다.
여행지 통영과 고향 속초를 섞지 않았고, 발표 일정만 삭제한 뒤 보리차 취향과 고양이 이름은 유지했다.
초기화 후에는 이전 이름을 답하지 않았다. 기억 추출 실패와 용량 초과에 따른 손실은 해당 시나리오에서 0건이었다.

60턴·199턴 검사는 기존 Gemma를 사용했다. 199턴 검사는 배포본과 같은 소스를 복사한
임시 loopback API `127.0.0.1:18012`에서 실행했다. STT·TTS를 생략하고 자동 기억 정리를 기다렸으며,
운영 대화 서버의 포트·설정은 유지했다. 반면 7턴 TTS 검사는 운영 8002에서 실행했고,
각 턴마다 명시적으로 기억 정리를 요청했다. Python 음성 검사에서는 PCM 확인 후 재생 완료를 모의로 전달했다.
실제 AudioSource 재생·정지·재개는 별도의 Unity 검사로 확인했다.

Unity 끼어들기 4개 경로에서는 다음 동작을 확인했다.

- 재개: 응답 ID와 PCM 커서를 유지하며 이어 재생.
- 수정·주제 전환: 기존 응답을 각각 한 번 취소한 뒤 새 응답 생성·재생.
- 대기: 요청할 때까지 정지 상태를 유지하고, 재개 요청 후 같은 응답을 이어 재생.
- 정지 중: PCM 소비 커서가 움직이지 않음.

서버 마지막 상태는 **14:00:37 KST** 기준이다. 대화 API는 `streaming_voice`,
TTS는 `generation_pcm / reference_icl`이고, 대화 연결 0개·TTS 참조 0개·TTS busy=false였다.
등록 API는 ready, 웹 `GET /gate`는 HTTP 200이었다. 초기 스냅샷에서 웹에 잘못된
`/_stcore/health` 경로를 사용해 받은 HTTP 오류는 실제 FastAPI 경로로 재확인했다.
검사 구간의 대화·TTS·TTS 엔진 로그에 새 ERROR·Traceback·CUDA OOM 표시는 없었다.
대화·TTS 서비스 PID와 시작 시각은 유지됐고, 배포 묶음의 23개 파일은 줄바꿈을 정규화한 해시가 일치했다.
검사용 API는 종료했고 18012 포트가 닫힌 것도 확인했다.

LLM 실행 프로세스의 부모 명령에서 `/home/crc_unity/models/gemma-4-31B-qat`를 확인했다.
health의 `exaone`은 `--served-model-name` 호환 이름이다.
검사 종료 시 GPU 사용량은 87,803 MiB, 여유는 9,447 MiB였으며 다른 사용자의 프로세스도 포함한다.
GPU 프로세스를 종료하거나 추가 모델을 로드하지 않았다.

Unity는 원래 `AI_Response_Test` 씬, Play 정지, 저장되지 않은 씬 변경 없음 상태로 복귀했다.
Test Runner가 13:43:06에 자동 저장한 설정 3개는 별도로 보관하고, 보관 당시와 바이트가 같은지 확인한 뒤
검사 전 내용으로 복구했다. 기능 코드나 설정을 바꿔 검사에 통과시킨 항목은 없다.

남은 제한과 관찰 사항은 다음과 같다.

- **추론 지연**: 본답변 첫 음성이 최대 9.55초였다. 리액션 이후 기다리는 시간의 편차가 남는다.
- **Quest Pro 미연결**: `adb devices`에 기기가 없었다. 기기 마이크·스피커·추적 데이터·APK 설치/실행은 미검증이다. 현재 Editor 빌드 대상은 Windows다.
- **품질 평가**: 실제 사람의 청취, 잡음 환경 STT 정확도, 화자 유사도, 얼굴·손가락 표현, 모든 프레임의 자연스러움은 자동 검사 통과와 별개다.
- **Unity MCP 로그**: Play 전환 중 `Client handler error: Cannot access a disposed object.`를 한 번 관찰했다. 이후 명령과 검사는 완료됐으며 애플리케이션 예외는 확인되지 않았다. 플러그인을 수정하지 않았다.
- **범위**: 기존 합의대로 Scene_1·Scene_2는 검사 대상에서 제외했다. 웹은 가용성·계약까지만 확인했으며 브라우저 설문 제출, 유료 Tripo 신규 생성, GitHub CI는 이번에 실행하지 않았다.
- **아직 없는 기능**: Quest 시선·고개·손동작을 활용한 신규 대화 기능은 구현되지 않아 이번 검사 대상이 아니다. 기억은 여전히 연결 단위이며 재접속 뒤 영구 보존하지 않는다.

원본 결과는 Git에서 제외되는 아래 폴더에 있다. 테스트 자료·음성·모델·비밀 설정을 커밋하지 않는다.

| 위치 | 결과 |
|---|---|
| `tools/_work/checks/20260915T043923Z-all-e8c07768/report.json` | 전체 모의 검사 125개 |
| `tools/_work/validation_20260915T0439/pipeline/report.json` | 실제 음성 9회·리액션·첫 음성 시간, 같은 폴더에 출력 WAV |
| 같은 검증 폴더의 `judge.json`, `memory.json`, `clarifications.json` | 실제 Gemma 판정·기억·되물음 결과 |
| `voice-memory.json`, `model-checks.json` | 실제 TTS 기억 연동, 실행 소스 해시·검사용 API 종료 |
| `ai-reference-preview.json`, `ai-reference-upload.json`, `ai-text-scene-smoke.json`, `semantic-interruption-check.json` | Unity 대화 검사 |
| `tripo-summary.json`, `tripo/<모델 폴더>/unity_motion*validation.json` | 기존 모델 2개의 편집/Play 검사 |
| `unity-baseline.json`, `unity-test-runner.json`, `unity-after.json`, `settings-cleanup.json` | Unity 전후 상태·0개 테스트 구분·설정 복구 |
| `server-before.json`, `server-after.json`, `llm-process-tree.json` | 서버 전후 가용성·프로세스·해시 |
| 서버 `/home/crc_unity/capstone-server/checks/validation_20260915T0439/` | 실제 모델 검사 로그와 원본 JSON, 검증에 쓴 소스 복사본 |

모의 검사는 프로젝트 루트에서 `python tools/check.py --area all`로 다시 실행할 수 있다.
실제 판정·기억 검사의 정식 진입점은 `tools/eval_turn_judge.py`, `tools/eval_dialogue_memory.py`,
`tools/eval_dialogue_clarifications.py`이며 새 출력 경로를 사용한다.
Unity 재실행은 [AI 테스트 씬](AI_응답_테스트_씬.md)의 메뉴와 사전 조건을 따른다.
새 PC의 환경 준비는 [공통 하네스](AI_하네스.md), 서버 설정은 [대화 AI 개발 가이드](대화_AI_개발가이드.md)를 따른다.
