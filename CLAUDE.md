# 다시, 봄 — Claude Code 진입점

@AGENTS.md

공통 작업 범위·서버 경계·검사·완료 기준은 위 파일에서 관리한다.
현재 기본 작업은 **대화 AI 개발**, T포즈·3D 제작은 팀원의 별도 작업이다.

Claude Code에서 사용할 진입점:

- `/check`: 로컬 모의 검사를 실행하고 결과를 확인한다. 기본 영역은 `dialogue`다.
- `/eval`: 기능 검사와 실제 대화 품질 평가를 구분해 진행한다.
- `/verify`: 요청받은 주장을 읽기 전용으로 독립 검증한다.
- `/impl`: 요청받은 대화 Python 구현안을 검토한 뒤 메인이 적용한다.
- `doc-scout`, `cross-check`: 사용자가 보조 조사를 요청했을 때 사용하는 읽기 전용 역할이다.

설정은 `.claude/settings.json`, 개인 설정은 `.claude/settings.local.json`이다.
프로젝트 루트에서 시작한다. 특정 PC의 절대 경로나 과거 샌드박스 실측을 공통 제약으로 사용하지 않는다.
현재 실행 방법과 결과 위치는 [AI 하네스](docs/AI_하네스.md)를 따른다.

## 2026-09-18 TTS 작업 재개

@docs/TTS_작업인계_20260918.md

Qwen 추가 원인 실험은 중단했다. 인계 문서의 최신 완료 상태와 사용자 승인 범위를 먼저 확인한다.
운영 TTS는 **VoxCPM2로 확정**됐다. 모델 교체·모델 비교를 먼저 제안하지 않고,
품질 개선은 인계 문서 7장의 참조 음성 기준으로 접근한다.
